import type { FC } from "react";
import Header from "../header/Header";
import { Box } from "@mui/material";

const Home: FC = () => {
  return (
    <Box>
      <Header />
    </Box>
  );
};

export default Home;
